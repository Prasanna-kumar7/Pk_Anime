import React from "react";
import ReactDOM from "react-dom/client";
import SearchAnimeApi from "./SearchAnime/SearchAnimeApi";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SearchAnimeApi />
  </React.StrictMode>
);
